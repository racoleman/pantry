## Prerequisites
Pantry-server requires a `config.js` file to connect to the database. Contact Rob for a copy.

## How to Run
```bash
cd server
npm install
npm start
```
```bash
cd client
npm install
npm start
```